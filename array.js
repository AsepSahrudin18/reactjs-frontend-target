// membuat variable array menggunakan const
const animals = ['ayam', 'kucing', 'kambing', 'sapi'];

// mengakses data array
console.log(animals[0], animals[1]);

// catatan: index array dimulai dari index ke 0